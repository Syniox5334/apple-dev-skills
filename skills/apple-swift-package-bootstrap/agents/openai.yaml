interface:
  display_name: "Apple Swift Package Bootstrap"
  short_description: "Create Swift package repos with deterministic defaults"
  default_prompt: "Use apple-swift-package-bootstrap for deterministic Swift package scaffolding with explicit inputs, script-grounded defaults, and clear validation output."
